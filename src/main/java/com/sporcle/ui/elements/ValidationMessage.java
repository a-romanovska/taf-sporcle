package com.sporcle.ui.elements;

import org.openqa.selenium.WebElement;

public class ValidationMessage extends BaseElement {
    public ValidationMessage(WebElement element) {
        super(element);
    }
}
